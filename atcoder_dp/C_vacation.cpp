#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<> v(n);
    for (int i = 0; i < n; i++)
    {
    }
    // cin >> v[i].a >> v[i].b >> v[i].c;

    return 0;
}